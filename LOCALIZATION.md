# الترجمة العربية والإنجليزية

يفتح مسار `/` صفحة `/login` أولًا، وتوجد الرئيسية في `/home`. وضع الدخول المؤقت مفعّل افتراضيًا: زر الدخول يفتح الرئيسية دون إرسال بيانات الحقول أو إنشاء جلسة مصادقة. لاستعادة نموذج تسجيل الدخول الحقيقي، أضف `DEMO_LOGIN_ENABLED=false` إلى `.env` ثم نفّذ `php artisan config:clear`.

- العربية هي اللغة الافتراضية (`APP_LOCALE=ar`)، والإنجليزية هي لغة الاحتياط (`APP_FALLBACK_LOCALE=en`).
- زر اللغة يرسل طلب `POST` محميًا بـ CSRF إلى `language/{locale}`، ويحفظ الاختيار في الجلسة، ثم يعود إلى الصفحة الحالية مع معلمات البحث. تُزال روابط الأقسام مثل `#faq` لمنع الانتقال التلقائي إليها عند تبديل اللغة.
- `SetLocale` تعمل بعد تحميل الجلسة ضمن مجموعة `web`، وتشمل طلبات تحديث Livewire. تضبط أيضًا لغة التواريخ النسبية.
- نصوص الصفحة الرئيسية وبيانات الفرص التجريبية موجودة في `lang/ar/site.php` و`lang/en/site.php`، وتُعرض باستخدام `__('site.key')`.
- نصوص صفحات المصادقة والإعدادات ومكونات Flux موجودة في `lang/ar.json` و`lang/en.json`، وتستخدم المفاتيح الإنجليزية الموجودة في المشروع.
- رسائل التحقق والمصادقة وإعادة تعيين كلمة المرور موجودة في ملفات PHP داخل مجلدي اللغتين. قوالب البريد المخصصة موجودة في `resources/views/vendor`.
- جميع قوالب الصفحات تضبط `lang` و`dir` بحسب اللغة. استخدم خصائص CSS المنطقية مثل `margin-inline-start` وفئات Tailwind مثل `ms-2` و`text-start` عند إضافة عناصر جديدة.

عند إضافة نص جديد، أضف ترجمتيه مع الحفاظ على أسماء المتغيرات مثل `:title` و`:time`. المحتوى المستقبلي المخزن في قاعدة البيانات يحتاج حقول ترجمة مستقلة؛ ملفات اللغة الحالية تغطي الواجهة والفرص التجريبية.

التحقق: `php artisan test --filter=LocalizationTest`، ثم `npm.cmd run build`.
